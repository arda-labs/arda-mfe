import { OffBalanceFormPage } from "../off-balance-form"

/** Nhập xuất ngoại bảng — init form on the tabbed posting-flow shell (FAC.201.01). */
export function OffBalancePostingInitPage() {
  return <OffBalanceFormPage />
}
