import { CancellationFormPage } from "../cancellation-form"

/** Hủy giao dịch — init form on the tabbed posting-flow shell (FAC.300.01). */
export function CancellationPostingInitPage() {
  return <CancellationFormPage />
}
