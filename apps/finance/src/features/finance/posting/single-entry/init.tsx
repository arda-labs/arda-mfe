import { PostingCaseInitPage } from "../posting-form"

/** Bút toán lẻ — init form on the shared posting-flow shell. */
export function SingleEntryPostingInitPage() {
  return <PostingCaseInitPage flow="SINGLE_ENTRY" />
}
