import { PostingCaseInitPage } from "../posting-form"

/** Bút toán kép — init form on the shared posting-flow shell. */
export function DoubleEntryPostingInitPage() {
  return <PostingCaseInitPage flow="DOUBLE_ENTRY" />
}
