import { ClosingFormPage } from "../closing-form"

/** Kết chuyển thu chi — init form on the tabbed posting-flow shell (FAC.203.01). */
export function ClosingPostingInitPage() {
  return <ClosingFormPage />
}
